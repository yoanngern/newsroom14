<!DOCTYPE html>
<html lang="fr">
    <head>
	<meta charset="utf-8">
        <title>{% block title %}Newsroom{% endblock %}</title>
        <link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon">
        <link rel="icon" type="image/png" href="images/favicon.png" />
        
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}" type="text/css" />
        {% endblock %}
        
        {% block javascripts %}
            <script type="text/javascript" src="{{ asset('js/jquery-1.11.0.js') }}"></script>
            <script type="text/javascript" src="{{ asset('js/app.js') }}"></script>
        {% endblock %}
        
    </head>
    <body>
        
        <div id="background"></div>
        
        <header>
            <nav>
                <ul>
                    <li><a href="{{ path('comem_newsroom_home') }}">Accueil</a></li>
                    
                    <li>
                        <h1>Problématique</h1>
                        <ul>
                            <li><a class="{% if page == 'alimentation' %}actif{% endif %}" href="{{ path('comem_newsroom_theme', {'theme': 'alimentation'}) }}">Alimentation</a></li>
                            <li><a class="{% if page == 'vetements' %}actif{% endif %}" href="{{ path('comem_newsroom_theme', {'theme': 'vetements'}) }}">Habits</a></li>
                            <li><a class="{% if page == 'logement' %}actif{% endif %}" href="{{ path('comem_newsroom_theme', {'theme': 'logement'}) }}">Logement</a></li>
                            <li><a class="{% if page == 'hygiene' %}actif{% endif %}" href="{{ path('comem_newsroom_theme', {'theme': 'hygiene'}) }}">Hygiène</a></li>
                            <li><a class="{% if page == 'social' %}actif{% endif %}" href="{{ path('comem_newsroom_theme', {'theme': 'social'}) }}">Relations sociales</a></li>
                        </ul>
                    </li>
                    <li>
                        <h1>Reportage</h1>
                        <ul>
                            <li><a class="{% if page == 'etudiants' %}actif{% endif %}" href="{{ path('comem_newsroom_report', {'title': 'etudiants'}) }}">Etudiants</a></li>
                            <li><a class="{% if page == 'statistiques' %}actif{% endif %}" href="{{ path('comem_newsroom_report', {'title': 'statistiques'}) }}">Statistiques</a></li>
                            <li><a class="{% if page == 'famille' %}actif{% endif %}" href="{{ path('comem_newsroom_report', {'title': 'famille'}) }}">Famille</a></li>
                        </ul>
                    </li>
                    <li><a href="{{ path('comem_newsroom_directory') }}">Annuaire</a></li>
                    <li>A propos</li>
                </ul>
            </nav>
        </header>
        
        <article id="main">
            {% block content %}
            {% endblock %}
        </article>
        
    </body>
</html>
